# dsh-desktop 桌面壳重构方向方案

> **文档版本：v1.0**（2026-09-07 创建）
> 用途：在「官方 dsh web 已启用 per-process token 认证」的前提下，决策桌面壳（`desktop/`）是继续做「壳内嵌 Web GUI」，还是转型为「启动管理器」。本文件先定方向，**暂不改代码**；待用户在另一台机器复核其壳改动后，再按选定路线实施。
> 关联交接：`HANDOVER.md` §21 / §21.5（本地已做适配与接管，提交 `3aa204a7bf`、`402fe67b02`，未推送）。

## 1. 背景与关键事实（2026-09-07 实测）

### 1.1 官方核心变化：dsh web 加认证门

- 自 **0.1.2-rc.1** 起，`dsh web` 对**无凭证的任何请求一律返回 401**（正文 `dsh web authentication required; reopen the URL printed by dsh web.`）。
- 机制（源码 `packages/client/connection/src/browser-auth.ts`）：
  - 每次进程启动**随机生成一个 launch token**（`randomBytes(32)`，仅存于进程内存 WeakMap，不落盘）；
  - 启动时打印带 token 的地址：`dsh web: http://127.0.0.1:PORT/?token=<base64url> (LAN: http://<IP>:PORT/?token=…)`（`printUrl` 默认 true，0.1.2-rc.1 安装版与 master 源码均确认）；
  - 用该 URL 访问一次 → 服务端签发按 authority（host）绑定的 HMAC cookie（`dsh-auth-*`，HttpOnly、SameSite=Strict、默认长有效期）→ 303 回干净的 `/` → 后续带 cookie 即通过；
  - 另默认 `openBrowser=true`：dsh web 启动后自动调用系统浏览器打开上述 URL（官方「启动即打开」的用法）。
- **推论（本次排查的核心结论）**：**只有「启动该 dsh web 进程的一方」且「能读到它 stdout」才拿得到 token**。官方产品形态实际就是 launch-and-open，并未按「第三方壳内嵌 WebView」设计；但底层不禁止嵌入——任何浏览器引擎（含 WebView2）走一遍 token→cookie 流程都能进。

### 1.2 本地壳失败链（为什么窗口进不去 GUI）

1. 壳 08-18 版 `waitReady` 只认 HTTP 200 → 新版 401 必超时弹错（已本地修：任意 HTTP 应答即就绪）。
2. 壳自拉 dsh web 的 stdout 重定向**不可靠**：spawn 链为 `cmd /C <dsh 命令>`（CREATE_NO_WINDOW|DETACHED_PROCESS）→ stdout/stderr append 到 `%APPDATA%\dsh-desktop\dsh.log`。实测多次自拉后 **dsh.log 里始终没有 token 行**（仅 08:29:55 追加过 `^C^C^C^C^C^C`，即只有进程被杀时的 ^C 回显到达了文件）→ `authURLFromLog` 无数据可解析。
3. 3080 长期被**非壳启动**的外部实例占用（启动者未把输出交给壳）→ 壳走「复用」→ token 不可得 → 只能回退系统浏览器（浏览器侧有 cookie，能用）。
4. 今天 08:03–09:47 期间出现多次实例更替（node 进程 6952→3924→6044→1288→7396→3936、壳 8812→4964→4500→6272），存在**用户反复重启 + 壳健康检查/接管 + 外部实例**相互打架的混乱状态。
5. 已本地补「外部实例掉线自动接管」（`recoverHarness` 端口真死后拉起自己的实例）——但**只要 stdout 捕获拿不到 token，接管后窗口仍进不去 GUI**，只能浏览器。

### 1.3 fork↔官方同步状态（用户问询）

- `sync-upstream.yml`（每日 08:00 北京 = UTC 00:00 cron）**运行正常**：09-02~09-06 连续 success。
- 官方 `upstream/master` = `d347e70390`（**官方自 09-04 起未再推送**）；fork `master` = `b49fd7b113` 已包含之（0 新提交）→ **fork 与官方对齐，无故障**。
- 本地 clone 较 fork 多 2 个本地未推送提交（本次壳适配）。

## 2. 路线 A（推荐）：转型「启动管理器」（Launcher）

### 2.1 定位

主窗口不再强行内嵌 GUI，而是**管理 dsh web 的生命周期与入口**，与官方 `dsh web`「启动→打印 URL→打开浏览器」的用法对齐，零对抗。

### 2.2 功能清单（主窗口 = 控制面板）

| 模块 | 说明 |
|---|---|
| 状态灯 | 运行中/停止/启动中/异常（轮询端口 + HTTP 探测，探测只判「有应答」） |
| 控制 | 开始 / 停止 / 重启 dsh web；退出壳时可选连带停止（默认保留官方“退出壳不杀已复用实例”语义需重定义——launcher 模式下建议：壳退=所管实例退） |
| 打开 Web UI | **直接 spawn node 进程并以 stdout 管道读 token URL** → 用系统浏览器打开带 token 地址；同时把 URL 显示/可复制到面板 |
| 日志 | dsh.log / 本次进程 stdout 实时查看（管道 + 追加到文件双写） |
| 配置 | 端口 / 命令 / 工作目录 / 开机自启 / 「是否尝试壳内嵌 WebView（可选，见 §4）」 |
| 通知 | 启动成功、崩溃自愈、端口被占提示 |
| 崩溃自愈 | 健康监测 5s；端口真死 → 自动再拉起（维持现有 owns 语义，launcher 永远 owns） |

### 2.3 关键技术点

- **spawn 方式**：直接定位 `@deepseek-ai/dsh` 的 `lib/bin.js`（`node <path> web [--port N]`），**不复用 `cmd /C`/ps1 shim**；stdout/stderr 用 Go `os/exec` **管道**实时读（可避免文件重定向与 shim 链的丢失），解析 `dsh web: http://…?token=…` 行。
- **凭证落点**：launcher 永远只开自己的实例（检测到 3080 被他人占用 → 提示「端口被 X 占用」，不静默复用、不杀他人进程；或提供「接管」按钮=提示后由用户确认再杀）。这样 token 永远可解析。
- **打开方式默认系统浏览器**（官方同款）；`openBrowser` 由壳自身完成即可避免 dsh web 自带 auto-open 与壳冲突（spawn 时传 `--no-open` 并自己打开，杜绝双开）。

### 2.4 成本 / 风险

- 成本：中等。控制面板前端重写（现有 Wails splash 前端可扩展成面板）；spawn+管道+解析（新 Go 代码）；launcher 语义回归测试。
- 风险：低。不依赖 WebView 认证细节；核心后续再改认证（如 token 格式/打印行变化）只需同步正则；官方怎么用我们就怎么用。
- 收益：窗口「必然可用」（打开浏览器这步官方自己都在做）；彻底摆脱「窗口内 401/白屏」类问题；天然兼容核心高频演进。

## 3. 路线 B：坚持「壳内嵌 GUI」加壳

### 3.1 前提（必须同时满足，缺一不可）

1. **壳独占 3080**：发现端口被外部实例占用时不可静默复用（要么等它死、要么提示用户、要么确认后接管杀掉）——因为外部实例的 token 拿不到。
2. **自拉实例的 token 必须可靠到手**：改用 §2.3 的直接 node spawn + stdout 管道解析（不能用现在的文件重定向链）。
3. 首访 token URL 后 cookie 由 WebView2 持有；壳重启且实例仍在时，WebView cookie 未过期可直进（依赖 WebView2 user-data 持久性，需实测确认）。
4. 每次核心发布都可能改认证行为 → 需跟进回归（成本外溢）。

### 3.2 成本 / 风险

- 成本：高。除 §2.3 全部工作外，还要处理 WebView2 cookie 持久化/失效、跳转时序、与核心版本耦合的持续回归；面板仍要保留兜底入口（打不开时开浏览器）。
- 风险：中高。今天已实测的坑（打印行进不了日志、实例抢占、反复重启混乱）都属此类；官方 pre-stable，认证语义仍可能变。
- 收益：保留「单窗口 App」体验（用户最初诉求）。若用户确需「看起来像独立应用、不跳浏览器」，才值得。

## 4. 混合形态（推荐路线 A 的落地变体）

- **默认**：Launcher（面板 + 系统浏览器打开，保证可用）。
- **可选开关「窗口内打开」**：开启时，壳用 §2.3 的 token（已从管道拿到）把 WebView 指到 token URL；失败/不可用时自动降级为系统浏览器并提示。两条腿都走，核心再怎么变都有兜底。

## 5. 决策对比速览

| 维度 | A Launcher（默认浏览器） | B 内嵌加壳 | A+ 混合（推荐） |
|---|---|---|---|
| 窗口内直达 GUI | ✕（浏览器） | ✓ | 可选 ✓ |
| 与官方演进对抗 | 无 | 持续跟进 | 极低 |
| 实现成本 | 中 | 高 | 中（先 A 后加开关） |
| 维护风险 | 低 | 中高 | 低 |
| 用户体验兜底 | 天然可用 | 坏了就白屏 | 双通道 |

## 6. 待决策 / 下一步

1. **方向确认**：选 A / B / A+（本文件建议 A+，先 A）。
2. **与另一台机器协调**：那边也有壳改动（疑同为适配认证，但未真正 push 到任何 GitHub 仓库）。实施前先 fetch/对比双方 `desktop/` diff，避免重复或冲突；建议由用户在 fork 上统一后再开工。
3. 若选 A/A+：更新本文件到 v2.0 附实施清单，再动代码；实现后按 §21 惯例本地提交→实机验证→再决定推送。

## 变更记录

- v1.0（2026-09-07）：初稿。依据当日实测（认证门机制、stdout 捕获失效、实例抢占、同步正常）与官方 0.1.2-rc.1 安装版源码。
